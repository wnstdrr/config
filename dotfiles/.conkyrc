conky.config = {
    -- Window setup (transparent, no-tiling)
    own_window_class       = 'Conky',
    own_window_type        = 'override',
    alignment              = 'bottom_left',
    own_window_hints       = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    gap_x                  = 0.0,
    gap_y                  = -1,
    minimum_size           = '20 20',
    maximum_width          = 250,

    background             = true,
    own_window             = true,
    own_window_argb_visual = true,
    own_window_transparent = true,
    draw_shades            = false,
    draw_outline           = false,
    draw_borders           = false,
    draw_graph_borders     = false,

    -- Buffering (update-interval, buffering-type, runtimes)
    update_interval = 1.0,
    total_run_times = 0,
    cpu_avg_samples = 2,
    no_buffers      = true,
    double_buffer   = true,

    -- Colors (default, custom)
    default_color         = '28a096',
    default_shade_color   = '000000',
    default_outline_color = 'd9d7d6',


    -- Font setup (xft)
    xftalpha             = 1.0,
    use_xft              = true,
    uppercase            = false,
    override_utf8_locale = false
}

conky.text = [[
    Filesystem Space Left:
    /$alignr${fs_used /}/${fs_size /}
    $fs_bar

    /home/cc$alignr${fs_used /home/cc}/${fs_size /home/cc}
    ${fs_bar /home/cc}

    Disk I/O:$alignr$diskio
    ${diskiograph /dev/nvme1n1}
]]
